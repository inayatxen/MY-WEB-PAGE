{
  "buildCommand": "pnpm build",
  "outputDirectory": "dist/spa",
  "env": {
    "PING_MESSAGE": "pong"
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/server/index.ts"
    }
  ]
}
